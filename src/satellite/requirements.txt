# AfriMine AI - Satellite Analysis Pipeline
# Install: pip install -r requirements.txt
#
# NOTE: GDAL Python bindings require system GDAL libraries.
#       Install via conda: conda install -c conda-forge gdal
#       or via system package manager before pip install.

# Google Earth Engine (optional — pipeline runs in offline mode without it)
earthengine-api>=0.1.370

# Raster & Vector Data
rasterio>=1.3.8,<1.5
geopandas>=0.13.0,<2.0
shapely>=2.0.0
fiona>=1.9.0,<2.0
pyproj>=3.6.0

# Image Processing
opencv-python>=4.8.0
scikit-image>=0.21.0,<0.25
scipy>=1.11.0,<2.0

# Numerical
numpy>=1.24.0,<3.0
pandas>=2.0.0,<3.0

# Visualization
matplotlib>=3.7.0,<4.0
contextily>=1.3.0
seaborn>=0.12.0

# Database (optional — falls back to in-memory mode)
psycopg2-binary>=2.9.7
sqlalchemy>=2.0.0

# Utilities
requests>=2.31.0
tqdm>=4.65.0
pyyaml>=6.0.0
python-dateutil>=2.8.0
